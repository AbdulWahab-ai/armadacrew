# Account security

Password resets never go through this copilot. Session revocation is a security queue ticket.

If a customer reports unexpected charges plus login failures, correlate with `auth-service` incidents before refunding — it may be account takeover, not a duplicate capture.

PII in email bodies (SSN, PAN, physical address) requires the email HITL gate even for tiny refunds.
